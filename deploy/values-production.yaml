# deploy/values-production.yaml - production overrides
replicaCount: 3
image:
  tag: "1.0.0"
  pullPolicy: Always
securityContext:
  runAsNonRoot: true
  readOnlyRootFilesystem: true
resources:
  limits:
    cpu: 1
    memory: 512Mi
ingress:
  enabled: true
  tls: true
